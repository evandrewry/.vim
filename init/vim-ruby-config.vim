" Turn on syntax folding in ruby files
let ruby_fold=1
" Open all folds when a ruby file is opened (otherwise, the file starts folded)
autocmd FileType ruby set foldlevel=99
" Turn on syntax highlighting for ruby operators (==, ||, &&, etc)
let ruby_operators=1
